```plantuml
@startuml
title Remote Management (UC-G5)

participant "System Admin" as Admin
participant Backend
participant GameClient
participant "Hardware Watchdog" as Watchdog

' Background: The GameClient is online and listening for commands (precondition)

box "Scenario Outline: Execute remote commands successfully" #LightBlue
    Admin -> Backend: Request to send "<command>"
    activate Backend
    note right of Backend: Examples: STOP, RESTART, UPDATE

    Backend -> GameClient: Send "<command>" signal
    activate GameClient
    note right of GameClient: Signal includes command and signature

    GameClient -> GameClient: Validate command signature
    GameClient -> GameClient: Execute "<action>" process
    note right of GameClient: Examples: End current session, Reboot application, Download patch files

    GameClient -> Backend: Report result (e.g., success, error)
    deactivate GameClient
    deactivate Backend
end box

newpage

box "Scenario: Watchdog activation on freeze" #LightPink
    activate GameClient
    GameClient -> GameClient: Attempts to restart
    note over GameClient: GameClient freezes during restart attempt (inactivity detected)
    deactivate GameClient

    activate Watchdog
    Watchdog -> Watchdog: Detects inactivity (e.g., heartbeat timeout)
    Watchdog -> GameClient: Force hard reset
    deactivate Watchdog
    note over GameClient: System reboots after hard reset
end box

@enduml
```