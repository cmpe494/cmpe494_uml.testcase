```plantuml
@startuml
' Actors
actor Admin

' Participants (components/services)
participant UserInterface as UI
participant BackendDistributionService as BDS
participant ZoneController as ZC
participant KioskDevice as KD

title UC-D6: Configuration Publishing - Basic Flow

Admin -> UI: Reviews pending configuration changes

activate UI
Admin -> UI: Clicks "Publish" button
note right: Trigger publishing event

UI -> BDS: POST /publish-configuration
deactivate UI

activate BDS
note over BDS: Initiates distribution to all components
BDS -> ZC: Distribute updated configuration (e.g., Zone-01)
activate ZC
ZC --> BDS: Configuration update acknowledged (ACK)
deactivate ZC

BDS -> ZC: Distribute updated configuration (e.g., Zone-02)
activate ZC
ZC --> BDS: Configuration update acknowledged (ACK)
deactivate ZC

' Represent multiple Kiosks with a loop or sequence
loop For each Kiosk
    BDS -> KD: Distribute updated configuration (e.g., Kiosk-01, Kiosk-02, ...)
    activate KD
    KD --> BDS: Configuration update acknowledged (ACK)
    deactivate KD
end

BDS --> UI: Publishing successful
deactivate BDS

activate UI
UI -> Admin: Displays "Configuration published successfully" message
deactivate UI

@enduml
```